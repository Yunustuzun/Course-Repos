<mvc:View controllerName="int.trial.first.ZINT_TRIAL_FIRST.controller.App" xmlns:mvc="sap.ui.core.mvc" displayBlock="true" xmlns="sap.m">
	<Shell id="shell">
		<App id="app">
			<pages>
				<Page id="page" title="{i18n>title}">
					<content>
						<HBox>
							<Button text="Default" press="onPress">
							</Button>
							<Button type="Accept" text="Accept" press="onPress">
							</Button>
							<Button type="Reject" text="Reject" press="onPress">
							</Button>
							<Button text="Coming Soon" press="onPress" enabled="false">
							</Button>
						</HBox>
						<Label id="genericButtonDescription" text="Örnek butonlar"/>
					</content>
				</Page>
			</pages>
		</App>
	</Shell>
</mvc:View>